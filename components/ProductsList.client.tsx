"use client"
import React from 'react'
import { ItemType } from '@/types/types'

const ProductsList = ({itemsArray}as{ItemType[]}) => {
  return (
    <div>ProductsList</div>
  )
}

export default ProductsList