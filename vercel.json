{
  "version": 2,
  "builds": [
    {
      "src": "app/api/**/*.ts",
      "use": "@vercel/node"
    },
    {
      "src": "lib/**/*",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "/api/$1" }]
}
